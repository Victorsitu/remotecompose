name: Convert Remote Compose Documents

on:
  push:
    branches: [main]
    paths:
      - '*.json'
      - 'server/**'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  convert:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'gradle'

      - name: Convert JSON configs and Kotlin components to binary .rc files
        run: |
          cd server
          chmod +x gradlew
          ./gradlew run --args="--dir $GITHUB_WORKSPACE $GITHUB_WORKSPACE"

      - name: Commit binary files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add *.rc
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Auto-generate binary .rc files"
            git push
          fi
